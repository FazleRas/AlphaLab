// Base URL for the AlphaLab backend API. Set REACT_APP_API_URL to point at a
// local backend (e.g. http://localhost:8001) without editing source.
const API = process.env.REACT_APP_API_URL || 'https://alphalab-backend.onrender.com';

export default API;
